# angular版本操作指南

## 生成器

```
  1. 以IDEA为例，右键目录选择新建 -> NX generate -> @schematics/angular:[name]
     来创建对应的angular组件
  2. 命令行： pnpm nx generate "@schematics/angular:module" --name=entry --project=remote --routing
